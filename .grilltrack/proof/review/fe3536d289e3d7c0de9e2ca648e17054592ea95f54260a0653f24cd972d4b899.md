# Ledger Cards review

Date: 2026-09-25

Decision: `ledger-cards-pilot`

Source identity: `sha256:fe3536d289e3d7c0de9e2ca648e17054592ea95f54260a0653f24cd972d4b899`

Verification: `.grilltrack/proof/ledger-cards/PROOF.md`

This identity is the working-tree product diff named in that proof. It is not a Git commit. Commit, push, and pull request were not authorized.

## Standards

The move follows `FEATURE_MAP.md`, `docs/architecture.md`, `COMPAT.md`, and `AGENTS.md`.

- Ledger Cards now owns `src/features/ledger-cards/` and is reached through `index.ts`, the package root, and `@dinkuskit/blocks/astro`.
- Declared shared dependencies are only `src/shared/links.ts` and `src/shared/portable-text.ts`.
- `scripts/check-architecture.mjs all` passed, including the six-pilot assertion in table order.
- No source comments were added.
- Stored fields and the EmDash `0.35.0` pin are unchanged.
- The package pack listing includes the feature files and no longer lists `src/astro/LedgerCards.astro`.

No standards finding.

## Source intent

The confirmed lock was a boundary move that preserves the card.

- Repeater order remains `code`, `title`, `body`, `ctaLabel`, `ctaHref`, and the field array matches the baseline text.
- Renderer markup and CSS match the baseline. A card still requires a title or a body. The code stays optional. The link still requires both a label and `safeCtaHref`. An empty grid still renders nothing.
- Public classes, `data-dinkus-block="ledger-cards"`, tokens, and the `dinkus-blocks` layer remain.
- The five earlier pilots still pass the full browser suite.
- Excluded work was not done: no schema, copy, visual, accessibility, media, upload, link-policy, shared-utility, or other-block migration, and no dependency change.

`FEATURE_MAP.md` now names the existing `h3`, paragraph, filter, and safe-link behavior in the compatibility cell. That documents the preserved surface. It does not change it.

The published screenshot includes the EmDash edit chip because the existing spec stays authenticated. The card grid itself matches the lock. This is a disclosed capture limit, not a product defect.

No source-intent finding.

## Adjudication

Result: `clean`.

Classifications: none.
