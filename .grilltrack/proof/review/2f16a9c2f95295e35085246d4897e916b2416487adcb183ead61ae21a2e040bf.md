# Service Area Map and Project Record review

Date: 2026-09-25

Decision: `service-map-project-record`

Source identity: `sha256:2f16a9c2f95295e35085246d4897e916b2416487adcb183ead61ae21a2e040bf`

Verification: `.grilltrack/proof/service-map-project-record/PROOF.md`

This identity is the whole uncommitted product tree, including the earlier feature moves. It is not a Git commit.

## Standards

Both moves follow `FEATURE_MAP.md`, `docs/architecture.md`, `COMPAT.md`, and `AGENTS.md`.

- Each feature is reached through its `index.ts`, the package root, and `@dinkuskit/blocks/astro`.
- Service Area Map declares only `src/shared/portable-text.ts`. Project Record declares `src/shared/links.ts` and `src/shared/portable-text.ts`.
- The architecture check requires all eleven blocks as migrated pilots, in table order, and it passed.
- No source comments were added. Stored fields and the EmDash `0.35.0` pin are unchanged.
- The package listing contains both feature directories and no longer lists the removed Astro renderers.
- `src/astro/` now contains only the public facade.

No standards finding.

## Source intent

Bobby asked to move the last two blocks behind features without changing what they store or render.

- Field arrays match the baseline. The map still uses a media picker. The project record still carries its annotation types.
- Renderer markup and CSS match the baseline. The map still shows an image or the placeholder, a caption, and legend labels. The project record still filters links through `safeCtaHref`, keeps its named slots, and still strips reserved annotation attributes.
- The Project Record import change is the boundary correction. It no longer imports the package root from inside the feature. The called helper is unchanged.
- The focused screenshots and the full 12-scenario browser run match that behavior.
- The unresolved Service Area Map image and the EmDash edit chip are fixture and capture limits, not renderer changes.

No source-intent finding.

## Adjudication

Result: `clean`.

Classifications: none.
