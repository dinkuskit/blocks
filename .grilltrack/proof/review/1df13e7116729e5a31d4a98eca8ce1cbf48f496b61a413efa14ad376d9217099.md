# Gallery Lanes, Search Board, and Dispatch review

Date: 2026-09-25

Decision: `lanes-search-dispatch`

Source identity: `sha256:1df13e7116729e5a31d4a98eca8ce1cbf48f496b61a413efa14ad376d9217099`

Verification: `.grilltrack/proof/lanes-search-dispatch/PROOF.md`

This identity is the uncommitted product tree, including the earlier Ledger Cards move. It is not a Git commit.

## Standards

The three moves follow `FEATURE_MAP.md`, `docs/architecture.md`, `COMPAT.md`, and `AGENTS.md`.

- Each feature is reached only through its `index.ts`, the package root, and `@dinkuskit/blocks/astro`.
- Declared shared dependencies are `src/shared/links.ts` and `src/shared/portable-text.ts`.
- The architecture check requires these three after the six earlier pilots, and it passed.
- Service Area Map and Project Record remain mapped at `src/astro/`.
- No source comments were added. Stored fields and the EmDash `0.35.0` pin are unchanged.
- The package listing contains the new feature files and no longer lists the three removed Astro files.

No standards finding.

## Source intent

Bobby asked to move Gallery Lanes, Search Board, and Dispatch behind features without changing what they store or render.

- Field arrays match the baseline.
- Renderer markup and CSS match the baseline. Gallery Lanes still keeps a lane with a label or an image and still wraps a safe href. Search Board still shows the conditional header and only links that have both a label and a safe href. Dispatch still shows the CTA, phone, and email only when `safeCtaHref` accepts them.
- The focused screenshots and the full 12-scenario browser run match that behavior.
- The broken Gallery Lanes image icons are the fixture URLs failing to load. The image field and `<img>` are unchanged.
- The EmDash edit chip is the existing authenticated public visit, not a renderer change.

No source-intent finding.

## Adjudication

Result: `clean`.

Classifications: none.
