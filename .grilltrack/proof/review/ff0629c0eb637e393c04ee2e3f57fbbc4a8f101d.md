# Immutable review of ff0629c0eb637e393c04ee2e3f57fbbc4a8f101d

## Repository standards axis

The first required fix closed the proven apostrophe bypass and its regression passed. The final immutable source still relies on punctuation heuristics for mixed Astro syntax, however. A direct probe with `<p>Quote: 'unfinished <!-- human --></p>` still missed the HTML comment, while `<p>Visible // text</p>` incorrectly reported a source comment.

Classification: `required_fix`.

The implementation otherwise continues to satisfy the repository's block architecture, exact package pins, compatibility covenant, test layout, and external-action gates.

## Confirmed source-intent axis

The feature map, CTA Band migration, public compatibility surfaces, real browser behavior, quick/full verifier, and CI coverage remain faithful to the confirmed lock. No block runtime code changed in the first repair.

The remaining false negative violates strict no-human-authored-comment enforcement, and the false positive violates the explicit requirement to avoid comment-like content being mistaken for a comment. A clean source-intent verdict is not possible until mixed Astro syntax is parsed by syntax context.

Classification: `required_fix`.

## Route

Use the exact Astro compiler parser already present in the pinned toolchain as a direct development dependency. Detect HTML comment nodes from its AST, scan frontmatter and expression code with the TypeScript parser, and scan style content separately. Preserve the narrow generated/compiler directive allowlist. Add regressions for the missed HTML comment and ordinary visible `//` text, rerun proportionate verification, commit the repair locally, and review the resulting full SHA again.

## Repair verification

The architecture gate now uses exact `@astrojs/compiler` `2.13.1` syntax trees. HTML comment nodes are reported directly; frontmatter, script elements, and expression attributes are checked through the TypeScript parser; CSS and SCSS style content use their own comment syntax. The dependency is exact and development-only.

Regressions prove that unmatched visible punctuation and ordinary visible `//` content no longer affect comment detection, while HTML, frontmatter, script, expression, style, TypeScript, and YAML comments are rejected. Comment-like strings, URLs, regexes, compiler directives, and the exact generated markers remain accepted.

The final `bin/verify-blocks quick` repair run passed in 7.55 seconds with 28 unit tests and all four architecture regression groups. `pnpm test:workflows` passed all ten workflow tests. `pnpm check` also passed after the exact dependency and lockfile update, including the fixture production build and package-content check. The existing EmDash build warnings were unchanged. No CTA runtime or renderer source changed, so the prior 12-scenario Playwright proof remains applicable.
