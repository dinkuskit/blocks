# Immutable review of 72569a835b3bb907c993eb80642a25290a82caf2

## Repository standards axis

The commit is based directly on the unchanged `origin/main` baseline, changes only the confirmed architecture slice, preserves exact dependency pins and package checks, keeps CTA Band in the required cascade layer with its existing public hooks and tokens, and does not include private data or external delivery changes.

One enforcement defect requires repair before the standards result can be clean. In an Astro template, an apostrophe in visible text could put the generic marker scanner into quoted-string state, allowing a later human-authored HTML comment to escape detection. A direct probe with `<p>It's text</p> <!-- human -->` returned no violation.

Classification: `required_fix`.

## Confirmed source-intent axis

CTA Band's stored fields, package exports, renderer markup, renderer styles, runtime behavior, real fixture flow, and the ten unmigrated block locations match the confirmed lock. The feature map and quick/full entry points are present.

The apostrophe bypass is also a source-intent failure because the accepted lock requires strict deterministic no-human-authored-comment enforcement without weakening the rule. It must be repaired and regression-tested before clean review.

Classification: `required_fix`.

## Route

Repair the quote-start logic so punctuation inside visible Astro text cannot mask later comments while real attribute or script strings remain exempt. Add accepted and rejected regression cases, rerun the quick verifier and proportionate package checks, create a follow-up local commit, and review the final full SHA again on both axes.

## Repair verification

The scanner now begins quoted-string state only in value contexts rather than at every apostrophe or quotation mark. The original bypass is rejected, while an apostrophe plus comment-like text inside a real attribute or Astro expression string remains accepted.

`bin/verify-blocks quick` passed in 7.40 seconds with 28 unit tests and all four architecture regression groups. `pnpm test:workflows` separately passed all ten workflow tests. `git diff --check` passed. No CTA runtime or renderer source changed, so the prior full Playwright proof remains applicable.
