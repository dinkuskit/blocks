# Section Header exact-source review

Date: 2026-08-29

Decision: `section-header-pilot`

Source identity: `git:cbfaa6b92abe70bf686c08a9c6e35923552f9f39`

Baseline: `git:7725b255f58f885d675480607c0326cf28d7f23a`

Result: clean

## Repository-standards axis

No findings were accepted. Section Header owns its contract, ordered fields, public feature entry, and renderer. Its only repository-shared dependency is `src/shared/portable-text.ts`. The package root, `src/types.ts`, and Astro facades remain intact. Architecture, no-comments, import-boundary, TypeScript, public-entrypoint, plugin-field, and theming checks passed.

## Confirmed-source-intent axis

No findings were accepted. The constant value and node interface are unchanged. The ordered four-field descriptor is text-identical to the baseline declaration. After changing only the type import from `../types` to `./contract`, the baseline and feature-local renderers both hash to `b2e7d46ab2a7d252f23ad69353bbdfa481db09d8f2b0838d133484217edd67df`. Markup, native header and level-two-heading semantics, conditional metadata, `aria-hidden`, classes, data hook, CSS layer, theme tokens, and defaults are therefore byte-equivalent. Package manifests, the lockfile, fixture seed, EmDash `0.35.0`, and prior-pilot source are unchanged.

## Adjudication

- Accepted findings: none.
- `required_fix`: none.
- `defer`: none.
- `human_gate`: none.
- Rejected false-positive hypotheses: renderer or theming drift, stored-schema drift, lost public exports, undeclared shared dependency, unrelated feature migration, dependency or version change, and delivery mutation.

## Independent checks and residual risk

The independent reviewer ran diff validation, root TypeScript checking, the architecture all-check, five architecture workflow tests, and 29 unit tests against the exact commit. It did not independently rerun the Astro fixture build or Playwright. The decision proof records the passing focused and full browser runs and direct local screenshot inspection; those ignored browser artifacts are not part of the pinned Git source.
